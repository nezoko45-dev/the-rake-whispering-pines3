services:
  - type: web
    name: the-rake-whispering-pines3-multiplayer
    runtime: node
    plan: free
    branch: main
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    autoDeploy: true
